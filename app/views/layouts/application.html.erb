<!DOCTYPE html>
<html class="h-full bg-white">
  <head>
    <title><%= content_for(:title) || "CampHelp" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <% if show_navigation? %>
      <div class="min-h-full">
        <header class="sticky top-0 z-50 border-b border-gray-200 bg-white dark:border-white/10 dark:bg-gray-900">
            <nav aria-label="Menu">
            <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
                <div class="flex h-16 justify-between">
                <div class="flex">
                    <div class="flex shrink-0 items-center">
                    <div class="relative">
                        <div class="inline-flex items-center px-1 pt-1 h-16">
                        <button type="button" class="flex items-center text-sm font-bold text-gray-900 hover:text-gray-700" onclick="toggleDropdown()">
                            <%= @campsite.name %>
                            <svg class="ml-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                            </svg>
                        </button>
                        </div>

                        <div id="campsite-dropdown" class="hidden absolute left-0 mt-2 w-56 origin-top-left rounded-md bg-white p-0 py-1 text-base shadow-lg outline-1 outline-black/5 border border-gray-200 focus:outline-none z-50">
                        <div class="py-1">
                            <% @user_campsites.each do |campsite| %>
                            <% if campsite.id == @campsite.id %>
                                <div class="flex items-center px-4 py-2 text-sm text-gray-900 bg-gray-50">
                                <svg class="w-4 h-4 mr-3 text-indigo-500" fill="currentColor" viewBox="0 0 20 20">
                                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                                </svg>
                                <%= campsite.name %>
                                <%= " (Owner)" if campsite.owner == Current.user %>
                                </div>
                            <% else %>
                                <a href="<%= campsite_path(campsite.slug) %>" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-gray-900">
                                <div class="w-4 h-4 mr-3"></div>
                                <%= campsite.name %>
                                <%= " (Owner)" if campsite.owner == Current.user %>
                                </a>
                            <% end %>
                            <% end %>
                        </div>

                        <div class="py-1">
                            <a href="<%= new_campsite_path %>" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-gray-900">
                            <svg class="w-4 h-4 mr-3 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                            </svg>
                            Add a campsite
                            </a>
                        </div>
                        </div>
                    </div>
                    </div>
                    <div class="hidden sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
                    <%= link_to campsite_path(@campsite.slug),
                        class: "inline-flex items-center border-b-2 #{'border-indigo-500 text-gray-900' if current_page?(campsite_path(@campsite.slug))} #{'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' unless current_page?(campsite_path(@campsite.slug))} px-1 pt-1 text-sm font-medium dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200" do %>
                        Issues
                    <% end %>
                    <a href="#" class="inline-flex items-center border-b-2 border-transparent px-1 pt-1 text-sm font-medium text-gray-500 hover:border-gray-300 hover:text-gray-700 dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200">Guides</a>
                    <a href="#" class="inline-flex items-center border-b-2 border-transparent px-1 pt-1 text-sm font-medium text-gray-500 hover:border-gray-300 hover:text-gray-700 dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200">Facilities</a>
                    <%= link_to settings_campsite_path(@campsite.slug),
                        class: "inline-flex items-center border-b-2 #{'border-indigo-500 text-gray-900' if current_page?(settings_campsite_path(@campsite.slug))} #{'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700' unless current_page?(settings_campsite_path(@campsite.slug))} px-1 pt-1 text-sm font-medium dark:text-gray-400 dark:hover:border-white/20 dark:hover:text-gray-200" do %>
                        Settings
                    <% end %>
                    </div>
                </div>
                <div class="hidden sm:ml-6 sm:flex sm:items-center">
                    <div class="grid grid-cols-1">
                    <%= form_with url: session_path, method: :delete, local: true, id: "logout-form", class: "hidden" do |form| %>
                    <% end %>

                    <div class="relative inline-block">
                        <button type="button" class="flex items-center rounded-md bg-white py-1.5 px-3 text-sm text-gray-900 outline-1 -outline-offset-1 outline-gray-300 focus-visible:outline-2 focus-visible:-outline-offset-2 focus-visible:outline-indigo-600 hover:bg-gray-50" onclick="toggleAccountDropdown()">
                        <%= Current.user.email_address %>
                        <svg class="ml-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                        </svg>
                        </button>

                        <div id="account-dropdown" class="hidden absolute right-0 mt-2 w-full origin-top-right rounded-md bg-white p-0 py-1 text-base shadow-lg outline-1 outline-black/5 border border-gray-200 focus:outline-none z-50">
                        <div class="py-1">
                            <a href="/account" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-gray-900">
                            <svg class="w-4 h-4 mr-3 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                            </svg>
                            Account
                            </a>
                            <a href="/billing" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-gray-900">
                            <svg class="w-4 h-4 mr-3 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"></path>
                            </svg>
                            Billing
                            </a>
                        </div>

                        <div class="border-t border-gray-100 py-1">
                            <button onclick="document.getElementById('logout-form').submit();" class="flex items-center w-full px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 hover:text-gray-900 text-left bg-transparent border-none cursor-pointer">
                            <svg class="w-4 h-4 mr-3 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013 3v1"></path>
                            </svg>
                            Log out
                            </button>
                        </div>
                        </div>
                    </div>
                    </div>
                </div>
                </div>
            </div>
            </nav>
        </header>

        <div class="py-10">
          <main>
            <%= yield %>
          </main>
        </div>
      </div>

      <script>
      function toggleDropdown() {
        const dropdown = document.getElementById('campsite-dropdown');
        dropdown.classList.toggle('hidden');
      }

      // Close dropdown when clicking outside
      document.addEventListener('click', function(event) {
        const dropdown = document.getElementById('campsite-dropdown');
        const button = event.target.closest('button');

        if (!button && !dropdown.contains(event.target)) {
          dropdown.classList.add('hidden');
        }
      });

      function toggleAccountDropdown() {
        const dropdown = document.getElementById('account-dropdown');
        dropdown.classList.toggle('hidden');
      }

      // Close dropdown when clicking outside
      document.addEventListener('click', function(event) {
        const accountDropdown = document.getElementById('account-dropdown');
        const button = event.target.closest('button');

        if (!button?.onclick?.toString().includes('toggleAccountDropdown') && !accountDropdown.contains(event.target)) {
          accountDropdown.classList.add('hidden');
        }
      });
      </script>
    <% else %>
      <%= yield %>
    <% end %>
  </body>
</html>
